/**
 * auth-token router
 */

import { factories } from '@strapi/strapi'

// @ts-ignore - Type will be generated after first build
export default factories.createCoreRouter('api::auth-token.auth-token');
