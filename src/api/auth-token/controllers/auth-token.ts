/**
 * auth-token controller
 */

import { factories } from '@strapi/strapi'

// @ts-ignore - Type will be generated after first build
export default factories.createCoreController('api::auth-token.auth-token');
