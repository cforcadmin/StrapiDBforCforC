/**
 * auth-token service
 */

import { factories } from '@strapi/strapi'

// @ts-ignore - Type will be generated after first build
export default factories.createCoreService('api::auth-token.auth-token');
